/* 下拉刷新容器 */
.page-scroll {
  background: var(--kuromi-gradient-bg);
  width: 100%;
  height: 100vh;
}

/* 页面容器 - 现代化设计 */
.page-container {
  background: var(--kuromi-gradient-bg);
  width: 100%;
  min-height: 100vh;
  padding: var(--kuromi-space-4);
  position: relative;
  overflow-x: hidden;
}

/* 用户状态栏 - 库洛米主题现代化 */
.user-status-bar {
  background: var(--kuromi-white);
  border: 1rpx solid var(--kuromi-gray-200);
  padding: var(--kuromi-space-6);
  margin-bottom: var(--kuromi-space-8);
  border-radius: var(--kuromi-radius-2xl);
  box-shadow: var(--kuromi-shadow-lg);
  position: relative;
  overflow: hidden;
  backdrop-filter: blur(20rpx);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.user-status-bar::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 6rpx;
  background: var(--kuromi-gradient-1);
  border-radius: var(--kuromi-radius-full) var(--kuromi-radius-full) 0 0;
}

.user-status-bar:hover {
  transform: translateY(-2rpx);
  box-shadow: var(--kuromi-shadow-xl);
}

/* 已登录用户信息 - 现代化设计 */
.user-info {
  display: flex;
  align-items: center;
  gap: var(--kuromi-space-4);
}

.user-avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: var(--kuromi-radius-full);
  border: 3rpx solid var(--kuromi-primary);
  box-shadow: var(--kuromi-shadow-md);
  object-fit: cover;
}

.user-details {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.user-nickname {
  color: var(--kuromi-gray-900);
  font-size: var(--kuromi-text-xl);
  font-weight: 600;
  line-height: var(--kuromi-leading-tight);
  margin-bottom: var(--kuromi-space-1);
}

.user-phone {
  color: var(--kuromi-gray-600);
  font-size: var(--kuromi-text-sm);
  line-height: var(--kuromi-leading-normal);
  display: inline-flex;
  align-items: center;
  gap: var(--kuromi-space-1);
}

.user-phone::before {
  content: '📱';
  font-size: var(--kuromi-text-base);
}

.logout-btn {
  background: var(--kuromi-gradient-1);
  color: var(--kuromi-white);
  border: none;
  border-radius: var(--kuromi-radius-full);
  font-size: var(--kuromi-text-sm);
  font-weight: 600;
  padding: var(--kuromi-space-3) var(--kuromi-space-4);
  box-shadow: var(--kuromi-shadow-md);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  min-height: 60rpx;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

.logout-btn:active {
  transform: translateY(2rpx) scale(0.98);
  box-shadow: var(--kuromi-shadow-sm);
}

/* 未登录提示 - 现代化设计 */
.login-prompt {
  display: flex;
  align-items: center;
  gap: var(--kuromi-space-4);
  padding: var(--kuromi-space-4);
}

.default-avatar-placeholder {
  width: 120rpx;
  height: 120rpx;
  border-radius: var(--kuromi-radius-full);
  border: 3rpx solid var(--kuromi-gray-300);
  background: var(--kuromi-gray-100);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: var(--kuromi-shadow-sm);
}

.default-avatar-placeholder::before {
  content: '👤';
  font-size: 48rpx;
  color: var(--kuromi-gray-400);
}

.prompt-text {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.prompt-title {
  color: var(--kuromi-gray-900);
  font-size: var(--kuromi-text-xl);
  font-weight: 600;
  line-height: var(--kuromi-leading-tight);
  margin-bottom: var(--kuromi-space-1);
}

.prompt-subtitle {
  color: var(--kuromi-gray-600);
  font-size: var(--kuromi-text-sm);
  line-height: var(--kuromi-leading-normal);
}

.login-btn {
  background: var(--kuromi-gradient-1);
  color: var(--kuromi-white);
  border: none;
  border-radius: var(--kuromi-radius-full);
  font-size: var(--kuromi-text-base);
  font-weight: 600;
  padding: var(--kuromi-space-3) var(--kuromi-space-6);
  box-shadow: var(--kuromi-shadow-md);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  min-height: 80rpx;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

.login-btn:active {
  transform: translateY(2rpx) scale(0.98);
  box-shadow: var(--kuromi-shadow-sm);
}

/* 英雄区域 - 现代化设计 */
.hero-section {
  background: var(--kuromi-white);
  border: 1rpx solid var(--kuromi-gray-200);
  border-radius: var(--kuromi-radius-3xl);
  padding: var(--kuromi-space-12) var(--kuromi-space-8);
  margin: var(--kuromi-space-8) 0;
  text-align: center;
  position: relative;
  overflow: hidden;
  box-shadow: var(--kuromi-shadow-xl);
  backdrop-filter: blur(20rpx);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.hero-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 8rpx;
  background: var(--kuromi-gradient-1);
  border-radius: var(--kuromi-radius-full) var(--kuromi-radius-full) 0 0;
}

.hero-section::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 600rpx;
  height: 600rpx;
  background: radial-gradient(circle, rgba(139, 0, 139, 0.05) 0%, transparent 70%);
  transform: translate(-50%, -50%);
  pointer-events: none;
}

.hero-section:hover {
  transform: translateY(-4rpx);
  box-shadow: var(--kuromi-shadow-2xl);
}

.hero-content {
  display: flex;
  flex-direction: column;
  padding: 0;
  position: relative;
  z-index: 2;
}

.hero-text {
  width: 100%;
  margin-bottom: var(--kuromi-space-8);
}

.hero-title {
  color: var(--kuromi-gray-900);
  font-size: var(--kuromi-text-4xl);
  font-weight: 700;
  line-height: var(--kuromi-leading-tight);
  margin-bottom: var(--kuromi-space-4);
  letter-spacing: -0.025em;
}

.hero-title-gradient {
  background: var(--kuromi-gradient-1);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  color: transparent;
}

.primary-text {
  color: var(--kuromi-primary);
}

.hero-description {
  color: var(--kuromi-gray-600);
  font-size: var(--kuromi-text-lg);
  line-height: var(--kuromi-leading-relaxed);
  margin-bottom: var(--kuromi-space-8);
  max-width: 600rpx;
  margin-left: auto;
  margin-right: auto;
}

.hero-buttons {
  display: flex;
  flex-wrap: nowrap;
  gap: var(--kuromi-space-4);
  justify-content: center;
}

.hero-btn {
  background: var(--kuromi-gradient-1);
  border: none;
  color: var(--kuromi-white);
  padding: var(--kuromi-space-4) var(--kuromi-space-8);
  border-radius: var(--kuromi-radius-full);
  font-size: var(--kuromi-text-lg);
  font-weight: 600;
  line-height: var(--kuromi-leading-normal);
  box-shadow: var(--kuromi-shadow-lg);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  min-height: 96rpx;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--kuromi-space-2);
}

.hero-btn::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(45deg, rgba(255, 255, 255, 0.2) 0%, transparent 100%);
  border-radius: inherit;
  opacity: 0;
  transition: opacity 0.3s ease;
}

.hero-btn:hover::before {
  opacity: 1;
}

.hero-btn:active {
  transform: translateY(2rpx) scale(0.98);
  box-shadow: var(--kuromi-shadow-md);
}

.hero-images {
  width: 100%;
  position: relative;
}

.main-hero-image {
  border-radius: 20rpx;
  overflow: hidden;
  box-shadow: 0 50rpx 100rpx -24rpx rgba(0, 0, 0, 0.25);
  transform: rotate(2deg);
}

.main-hero-image image {
  width: 100%;
  height: 400rpx;
}

.secondary-hero-image {
  position: absolute;
  bottom: -48rpx;
  left: -48rpx;
  background-color: white;
  padding: 32rpx;
  border-radius: 16rpx;
  box-shadow: 0 40rpx 50rpx -10rpx rgba(0, 0, 0, 0.1), 0 20rpx 20rpx -10rpx rgba(0, 0, 0, 0.04);
  transform: rotate(-3deg);
}

.secondary-hero-image image {
  width: 200rpx;
  height: 200rpx;
  border-radius: 10rpx;
}

/* 占位符样式 */
.main-hero-placeholder {
  border-radius: 20rpx;
  overflow: hidden;
  box-shadow: 0 50rpx 100rpx -24rpx rgba(0, 0, 0, 0.25);
  transform: rotate(2deg);
  height: 400rpx;
  background: #f3f4f6;
  display: flex;
  align-items: center;
  justify-content: center;
}

.secondary-hero-placeholder {
  position: absolute;
  bottom: -48rpx;
  left: -48rpx;
  background-color: white;
  padding: 32rpx;
  border-radius: 16rpx;
  box-shadow: 0 40rpx 50rpx -10rpx rgba(0, 0, 0, 0.1), 0 20rpx 20rpx -10rpx rgba(0, 0, 0, 0.04);
  transform: rotate(-3deg);
  width: 200rpx;
  height: 200rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.placeholder-content {
  color: #9ca3af;
  font-size: 24rpx;
  text-align: center;
}

.promotion-placeholder {
  width: 100%;
  height: 200rpx;
  background: #f9fafb;
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 2rpx dashed #d1d5db;
}

/* 促销区域 */
.promotion-section {
  padding: 128rpx 0;
  background-color: var(--neutral);
}

.promotion-cards {
  display: flex;
  flex-direction: column;
  gap: 64rpx;
}

.promotion-card {
  background-color: white;
  border-radius: 16rpx;
  overflow: hidden;
  box-shadow: 0 20rpx 30rpx -6rpx rgba(0, 0, 0, 0.1), 0 8rpx 12rpx -4rpx rgba(0, 0, 0, 0.05);
}

.promotion-image-container {
  position: relative;
}

.promotion-image {
  width: 100%;
  height: 400rpx;
}

.promotion-badge {
  position: absolute;
  top: 32rpx;
  left: 32rpx;
  background-color: var(--accent);
  color: white;
  font-weight: bold;
  padding: 8rpx 24rpx;
  border-radius: 999rpx;
}

.promotion-content {
  padding: 48rpx;
}

.promotion-title {
  font-weight: bold;
  font-size: 36rpx;
  margin-bottom: 16rpx;
}

.promotion-description {
  color: #6B7280;
  margin-bottom: 32rpx;
}

.promotion-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.price-container {
  display: flex;
  align-items: center;
}

.old-price {
  color: #9CA3AF;
  text-decoration: line-through;
}

.new-price {
  color: var(--accent);
  font-weight: bold;
  font-size: 36rpx;
  margin-left: 16rpx;
}

.primary-button-sm {
  background-color: var(--primary);
  color: white;
  font-weight: 500;
  padding: 16rpx 32rpx;
  border-radius: 999rpx;
  font-size: 28rpx;
}

/* 产品分类 */
.categories-section {
  padding: 128rpx 0;
}

.categories-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 32rpx;
}

.category-card {
  cursor: pointer;
}

.category-image-container {
  position: relative;
  overflow: hidden;
  border-radius: 16rpx;
  box-shadow: 0 8rpx 12rpx -2rpx rgba(0, 0, 0, 0.1), 0 4rpx 8rpx -2rpx rgba(0, 0, 0, 0.06);
  margin-bottom: 32rpx;
}

.category-image {
  width: 100%;
  height: 320rpx;
  transition: transform 0.5s ease;
}

.category-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(to top, rgba(0, 0, 0, 0.7), transparent);
  opacity: 0.7;
}

.category-info {
  position: absolute;
  bottom: 32rpx;
  left: 32rpx;
  color: white;
}

.category-title {
  font-weight: bold;
  font-size: 32rpx;
}

.category-count {
  color: rgba(255, 255, 255, 0.8);
  font-size: 24rpx;
}

/* 热门产品 - 现代化设计 */
.products-section {
  padding: var(--kuromi-space-16) 0;
  background: var(--kuromi-gradient-bg);
}

.popular-products {
  margin: var(--kuromi-space-12) 0;
}

.section-title {
  font-size: var(--kuromi-text-3xl);
  font-weight: 700;
  color: var(--kuromi-gray-900);
  text-align: center;
  line-height: var(--kuromi-leading-tight);
  margin-bottom: var(--kuromi-space-8);
  position: relative;
  letter-spacing: -0.025em;
}

.section-title-primary {
  color: var(--kuromi-primary);
}

.section-title-gradient {
  background: var(--kuromi-gradient-1);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  color: transparent;
}

.section-title::before {
  content: '✨';
  position: absolute;
  left: -80rpx;
  top: 50%;
  transform: translateY(-50%);
  font-size: var(--kuromi-text-xl);
  opacity: 0.8;
}

.section-title::after {
  content: '✨';
  position: absolute;
  right: -80rpx;
  top: 50%;
  transform: translateY(-50%);
  font-size: var(--kuromi-text-xl);
  opacity: 0.8;
}

/* 产品网格 - 现代化设计 */
.products-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: var(--kuromi-space-4);
}

@media (min-width: 768px) {
  .products-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: var(--kuromi-space-6);
  }
}

@media (min-width: 1024px) {
  .products-grid {
    grid-template-columns: repeat(4, 1fr);
  }
}

/* 产品卡片 - 现代化设计 */
.product-card {
  background: var(--kuromi-white);
  border: 1rpx solid var(--kuromi-gray-200);
  border-radius: var(--kuromi-radius-xl);
  overflow: hidden;
  box-shadow: var(--kuromi-shadow-sm);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  backdrop-filter: blur(10rpx);
}

.product-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4rpx;
  background: var(--kuromi-gradient-1);
  opacity: 0;
  transition: opacity 0.3s ease;
}

.product-card:hover {
  transform: translateY(-4rpx);
  box-shadow: var(--kuromi-shadow-lg);
  border-color: var(--kuromi-gray-300);
}

.product-card:hover::before {
  opacity: 1;
}

.product-card:active {
  transform: translateY(-2rpx) scale(0.98);
  box-shadow: var(--kuromi-shadow-md);
}

/* 产品图片容器 - 现代化设计 */
.product-image-container {
  position: relative;
  width: 100%;
  height: 240rpx;
  border-radius: var(--kuromi-radius-lg);
  overflow: hidden;
  margin-bottom: var(--kuromi-space-4);
  background: var(--kuromi-gray-100);
}

.product-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.product-card:hover .product-image {
  transform: scale(1.05);
}

.product-image-placeholder {
  width: 100%;
  height: 100%;
  background: var(--kuromi-gray-100);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--kuromi-gray-400);
  font-size: var(--kuromi-text-4xl);
}

.product-actions {
  position: absolute;
  top: var(--kuromi-space-3);
  right: var(--kuromi-space-3);
  display: flex;
  flex-direction: column;
  gap: var(--kuromi-space-2);
}

.action-btn {
  width: 72rpx;
  height: 72rpx;
  border-radius: var(--kuromi-radius-full);
  background: rgba(255, 255, 255, 0.95);
  border: 1rpx solid var(--kuromi-gray-200);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--kuromi-text-lg);
  color: var(--kuromi-gray-600);
  box-shadow: var(--kuromi-shadow-md);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  backdrop-filter: blur(20rpx);
}

.action-btn:hover {
  background: var(--kuromi-primary);
  color: var(--kuromi-white);
  border-color: var(--kuromi-primary);
  transform: scale(1.1);
}

.action-btn:active {
  transform: scale(0.95);
}

.action-button {
  background-color: white;
  color: #6B7280;
  padding: 16rpx;
  border-radius: 50%;
  box-shadow: 0 8rpx 12rpx -2rpx rgba(0, 0, 0, 0.1), 0 4rpx 8rpx -2rpx rgba(0, 0, 0, 0.06);
}

.action-button-eye {
  color: var(--primary);
}

.product-add-to-cart {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background-color: rgba(22, 93, 255, 0.9);
  color: white;
  padding: 16rpx 32rpx;
  transform: translateY(100%);
  text-align: center;
  font-weight: 500;
}

/* 产品内容区域 - 现代化设计 */
.product-content {
  padding: var(--kuromi-space-6);
  background: var(--kuromi-white);
  position: relative;
}

.product-rating {
  display: flex;
  align-items: center;
  margin-bottom: 16rpx;
}

.rating-stars {
  display: flex;
  margin-right: 12rpx;
  font-size: 24rpx;
}

.rating-stars text {
  color: #ffc107;
  margin-right: 2rpx;
}

.rating-count {
  color: #9CA3AF;
  font-size: 24rpx;
  margin-left: 16rpx;
}

.product-title {
  font-weight: 600;
  font-size: var(--kuromi-text-lg);
  color: var(--kuromi-gray-900);
  line-height: var(--kuromi-leading-tight);
  margin-bottom: var(--kuromi-space-2);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.product-description {
  color: var(--kuromi-gray-600);
  font-size: var(--kuromi-text-sm);
  line-height: var(--kuromi-leading-relaxed);
  margin-bottom: var(--kuromi-space-4);
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.product-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.product-price {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: var(--kuromi-space-1);
}

.current-price {
  color: var(--kuromi-primary);
  font-weight: 700;
  font-size: var(--kuromi-text-xl);
  line-height: var(--kuromi-leading-none);
}

.original-price {
  color: var(--kuromi-gray-400);
  font-size: var(--kuromi-text-sm);
  text-decoration: line-through;
  line-height: var(--kuromi-leading-none);
}

.add-to-cart-button {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 80rpx;
  height: 80rpx;
  background: var(--kuromi-gradient-1);
  border: none;
  border-radius: var(--kuromi-radius-full);
  box-shadow: var(--kuromi-shadow-lg);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
}

.add-to-cart-button::before {
  content: '💜';
  position: absolute;
  top: -12rpx;
  right: -12rpx;
  font-size: var(--kuromi-text-xs);
  animation: float 2s ease-in-out infinite;
  z-index: 2;
}

.add-to-cart-button::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(45deg, rgba(255, 255, 255, 0.2) 0%, transparent 100%);
  border-radius: inherit;
  opacity: 0;
  transition: opacity 0.3s ease;
}

.add-to-cart-button:hover::after {
  opacity: 1;
}

.add-to-cart-button:active {
  transform: scale(0.95) translateY(2rpx);
  box-shadow: var(--kuromi-shadow-md);
}

.add-to-cart-button .cart-icon {
  width: 48rpx;
  height: 48rpx;
  filter: brightness(0) invert(1);
  z-index: 1;
}

.add-to-cart-button.loading {
  background: var(--kuromi-gray-400);
  pointer-events: none;
  animation: pulse 1.5s ease-in-out infinite;
}

.add-to-cart-button.loading text {
  color: var(--kuromi-white);
  font-size: var(--kuromi-text-xs);
  font-weight: 600;
  z-index: 1;
}

@keyframes float {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-4px); }
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.7; }
}

.text-center {
  text-align: center;
  margin-top: 64rpx;
}

.view-all-button {
  display: inline-flex;
  align-items: center;
  background: transparent;
  border: 4rpx solid var(--kuromi-primary);
  color: var(--kuromi-primary);
  font-weight: 600;
  padding: 24rpx 64rpx;
  border-radius: 50rpx;
  transition: all 0.3s ease;
  box-shadow: var(--kuromi-shadow-soft);
  position: relative;
}

.view-all-button::before {
  content: '🎀';
  margin-right: 8rpx;
  font-size: 20rpx;
}

.view-all-button:active {
  background: var(--kuromi-primary);
  color: white;
  transform: translateY(2rpx);
  box-shadow: var(--kuromi-glow-purple);
}

/* 图标样式 */
.icon-heart, .icon-heart-o, .icon-eye, .icon-star, 
.icon-star-o, .icon-star-half-o, .icon-shopping-cart, .icon-arrow-right {
  font-family: inherit;
}

.icon-heart:before {
  content: "❤️";
}

.icon-heart-o:before {
  content: "🤍";
}

.icon-eye:before {
  content: "👁️";
}

.icon-star::after {
  content: '★';
  color: #ffc107;
  font-size: 24rpx;
}

.icon-star-o::after {
  content: '☆';
  color: #ddd;
  font-size: 24rpx;
}

.icon-star-half-o::after {
  content: '★';
  color: #ffc107;
  font-size: 24rpx;
}

.icon-shopping-cart:before {
  content: "\f07a";
}

.icon-arrow-right:before {
  content: "\f061";
}

/* 响应式调整 */
@media (min-width: 768px) {
  .hero-content {
    flex-direction: row;
    align-items: center;
  }
  
  .hero-text {
    width: 50%;
    margin-bottom: 0;
  }
  
  .hero-images {
    width: 50%;
  }
  
  .promotion-cards {
    flex-direction: row;
  }
  
  .categories-grid {
    grid-template-columns: repeat(4, 1fr);
  }
  
  .products-grid {
    grid-template-columns: repeat(4, 1fr);
  }
}

/* 分页控制样式 */
.pagination-container {
  margin: 64rpx 0;
  padding: 32rpx;
  background-color: #f8f9fa;
  border-radius: 16rpx;
  text-align: center;
}

.pagination-info {
  font-size: 28rpx;
  color: #6B7280;
  margin-bottom: 32rpx;
}

.pagination-buttons {
  display: flex;
  justify-content: center;
  gap: 32rpx;
}

.pagination-btn {
  padding: 20rpx 40rpx;
  font-size: 28rpx;
  border-radius: 12rpx;
  border: 2rpx solid var(--primary);
  background-color: var(--primary);
  color: white;
  min-width: 160rpx;
  transition: all 0.3s ease;
}

.pagination-btn:not(.disabled):active {
  transform: scale(0.95);
  background-color: #0056b3;
}

.pagination-btn.disabled {
  background-color: #e9ecef;
  border-color: #e9ecef;
  color: #6c757d;
  opacity: 0.6;
}

.pagination-btn::after {
  border: none;
}