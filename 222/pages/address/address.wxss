/* 地址管理页面样式 - 库洛米主题 */
.page-container {
  min-height: 100vh;
  background: linear-gradient(135deg, #f8f0ff 0%, #ffe8f5 50%, #f0f8ff 100%);
  padding-bottom: 120rpx;
  position: relative;
}

.page-container::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-image: 
    radial-gradient(circle at 20% 80%, rgba(255, 105, 180, 0.12) 0%, transparent 50%),
    radial-gradient(circle at 80% 20%, rgba(139, 0, 139, 0.12) 0%, transparent 50%);
  pointer-events: none;
  z-index: -1;
}

.page-container::after {
  content: '📍💜';
  position: absolute;
  top: 6%;
  right: 6%;
  font-size: 26rpx;
  animation: float 2.8s ease-in-out infinite;
  z-index: 1;
}

@keyframes float {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-8px); }
}

/* 地址列表 */
.address-list {
  padding: 20rpx;
}

/* 地址卡片 - 库洛米主题 */
.address-item {
  background: white;
  border: 3rpx solid rgba(255, 105, 180, 0.2);
  border-radius: 25rpx;
  margin-bottom: 20rpx;
  padding: 30rpx;
  box-shadow: 0 4rpx 20rpx rgba(255, 105, 180, 0.15), 0 2rpx 8rpx rgba(139, 0, 139, 0.1);
  position: relative;
  transition: all 0.3s ease;
}

.address-item::before {
  content: '🏠';
  position: absolute;
  top: 20rpx;
  right: 20rpx;
  font-size: 22rpx;
  opacity: 0.7;
}

.address-item:hover {
  transform: translateY(-4rpx);
  box-shadow: 0 8rpx 32rpx rgba(255, 105, 180, 0.25), 0 4rpx 16rpx rgba(139, 0, 139, 0.15);
  border-color: #ff69b4;
}

.address-item.default {
  border: 2rpx solid #165DFF;
}

.address-content {
  margin-bottom: 20rpx;
}

.address-header {
  display: flex;
  align-items: center;
  margin-bottom: 16rpx;
}

.receiver-name {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
  margin-right: 20rpx;
}

.receiver-phone {
  font-size: 28rpx;
  color: #666;
  flex: 1;
}

.default-tag {
  background: #165DFF;
  color: white;
  font-size: 22rpx;
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
  margin-left: auto;
}

.address-detail {
  font-size: 28rpx;
  color: #666;
  line-height: 1.5;
}

.address-actions {
  display: flex;
  gap: 20rpx;
}

.action-btn {
  flex: 1;
  height: 60rpx;
  line-height: 60rpx;
  border-radius: 8rpx;
  font-size: 26rpx;
}

.edit-btn {
  background: #f0f8ff;
  color: #165DFF;
  border: 1rpx solid #165DFF;
}

.delete-btn {
  background: #fff5f5;
  color: #ff4757;
  border: 1rpx solid #ff4757;
}

/* 空状态 */
.empty-state {
  text-align: center;
  padding: 120rpx 40rpx;
}

.empty-icon {
  font-size: 120rpx;
  margin-bottom: 30rpx;
}

.empty-text {
  font-size: 32rpx;
  color: #333;
  margin-bottom: 16rpx;
}

.empty-desc {
  font-size: 28rpx;
  color: #999;
}

/* 新增地址按钮 */
.add-address-btn {
  position: fixed;
  bottom: 40rpx;
  left: 40rpx;
  right: 40rpx;
  height: 88rpx;
  background: #165DFF;
  border-radius: 44rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-size: 32rpx;
  box-shadow: 0 8rpx 24rpx rgba(22, 93, 255, 0.3);
}

.add-icon {
  font-size: 36rpx;
  margin-right: 12rpx;
}

/* 弹窗样式 */
.address-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 1000;
}

.modal-mask {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
}

.modal-content {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: white;
  border-radius: 24rpx 24rpx 0 0;
  max-height: 80vh;
  overflow: hidden;
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 30rpx 40rpx;
  border-bottom: 1rpx solid #eee;
}

.modal-title {
  font-size: 36rpx;
  font-weight: 600;
  color: #333;
}

.modal-close {
  font-size: 48rpx;
  color: #999;
  width: 60rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 表单样式 */
.form-content {
  padding: 40rpx;
  max-height: 60vh;
  overflow-y: auto;
}

.form-item {
  margin-bottom: 40rpx;
}

.form-label {
  display: block;
  font-size: 28rpx;
  color: #333;
  margin-bottom: 16rpx;
  font-weight: 500;
}

.form-input {
  width: 100%;
  height: 80rpx;
  padding: 0 24rpx;
  border: 1rpx solid #ddd;
  border-radius: 12rpx;
  font-size: 28rpx;
  background: #fafafa;
}

.form-input:focus {
  border-color: #165DFF;
  background: white;
}

.picker-text {
  height: 80rpx;
  line-height: 80rpx;
  padding: 0 24rpx;
  border: 1rpx solid #ddd;
  border-radius: 12rpx;
  font-size: 28rpx;
  background: #fafafa;
  color: #333;
}

.picker-text.placeholder {
  color: #999;
}

.form-textarea {
  width: 100%;
  min-height: 120rpx;
  padding: 20rpx 24rpx;
  border: 1rpx solid #ddd;
  border-radius: 12rpx;
  font-size: 28rpx;
  background: #fafafa;
  resize: none;
}

.form-textarea:focus {
  border-color: #165DFF;
  background: white;
}

.checkbox-item {
  display: flex;
  align-items: center;
}

.checkbox-label {
  margin-left: 16rpx;
  font-size: 28rpx;
  color: #333;
}

/* 表单按钮 */
.form-actions {
  display: flex;
  gap: 20rpx;
  padding: 30rpx 40rpx;
  border-top: 1rpx solid #eee;
}

.cancel-btn {
  flex: 1;
  height: 80rpx;
  line-height: 80rpx;
  background: #f5f5f5;
  color: #666;
  border: none;
  border-radius: 12rpx;
  font-size: 28rpx;
}

.confirm-btn {
  flex: 2;
  height: 80rpx;
  line-height: 80rpx;
  background: #165DFF;
  color: white;
  border: none;
  border-radius: 12rpx;
  font-size: 28rpx;
}

.confirm-btn[disabled] {
  background: #ccc;
}

/* 加载状态 */
.loading-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.3);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 2000;
}

.loading-content {
  background: white;
  padding: 40rpx;
  border-radius: 16rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.loading-spinner {
  width: 60rpx;
  height: 60rpx;
  border: 4rpx solid #f3f3f3;
  border-top: 4rpx solid #165DFF;
  border-radius: 50%;
  animation: spin 1s linear infinite;
  margin-bottom: 20rpx;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.loading-text {
  font-size: 28rpx;
  color: #666;
}